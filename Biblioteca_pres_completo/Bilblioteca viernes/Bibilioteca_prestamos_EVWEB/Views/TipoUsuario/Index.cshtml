@model IEnumerable<Bibilioteca_prestamos_EVWEB.Models.TipoUsuario>
@{
    ViewBag.Title = "Index";
}

<h1 class="text-center">Lista de Tipos de Usuarios</h1>
<div class="mb-2 text-end">
    <a href="/TipoUsuario/Create/" class="btn btn-primary">Crear Nuevo Tipo de Usuario</a>
</div>
<div class="table-responsive">
    <table class="table table-bordered table-hover table-striped">
        <thead class="table-dark">
            <tr>
                <th>Id</th>
                <th>Tipo Usuario</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.id_tipo</td>
                    <td>@item.Nombre</td>
                    <td>
                        <a href="/TipoUsuario/Edit/@item.id_tipo" class="btn btn-warning">EDITAR</a>
                        <a href="/TipoUsuario/Delete/@item.id_tipo" class="btn btn-danger">ELIMINAR</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

